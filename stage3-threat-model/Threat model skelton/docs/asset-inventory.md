# Asset Inventory

**Source of truth:** built from `stage2/.../docs/architecture.md`,
`network-design.md`, `security-hardening.md`, and `stage1/.../Project/outputs/`.
**Rule:** an asset's *data/sensitivity* here must match the owning stage's
docs — if they disagree, fix the stage doc, not this file.

Legend — **C** = confidentiality, **I** = integrity, **A** = availability
(CIA triad) · **Value** = what it is worth to the lab narrative.

## A. Current assets (Stages 1–2, built and verified)

| ID | Asset | Owner | Description | Data / sensitivity | CIA | Value |
|---|---|---|---|---|---|---|
| A-01 | `securebank-srv` host | Stage 2 | The hardened server VM (Debian/Ubuntu), hostname `securebank-srv.securebank.lab`, 10.10.10.10 | Host OS, users, configs, logs | I/A | **Critical** — hosts every later stage |
| A-02 | SSH service (port 22) | Stage 2 | OpenSSH per `99-securebank.conf` baseline; IPv4 + IPv6; banner | Auth events, keys | A/C | **High** — the admin entry point |
| A-03 | `securebank-admin` account | Stage 2 | Admin user in `sudo` group; only SSH-allowed account (AllowUsers) | Credentials, sudo rights | C/I | **Critical** |
| A-04 | SSH key material | Stage 2 | Local ed25519 (loopback, C-12) + Kali-provisioned keys; host keys (T-16) | Private keys | C | **High** |
| A-05 | nftables firewall | Stage 2 | Default-deny `inet` table, IPv4+IPv6, SSH from 10.10.10.0/24, `SB-DROP` logging | Ruleset | I | **High** |
| A-06 | sysctl baseline | Stage 2 | rp_filter, redirect/source-route off, syncookies, log_martians | Kernel params | I | Medium |
| A-07 | `/etc/hosts` managed block | Stage 2 | Generated by `setup.sh` from `lab.env` (BEGIN/END markers) | Name↔IP mapping | I | Medium |
| A-08 | journald logs (persistent) | Stage 2 | SSH auth, services, `SB-DROP` drops; `Storage=persistent` (C-15) | Evidence | I/A | **High** — Stage 8 input |
| A-09 | Forensics kit | Stage 2 | `collect-forensics.sh` snapshots: hashes, packages, host keys, journal excerpts | Evidence | I | Medium |
| A-10 | Setup scripts + configs | Stage 2 | `setup.sh`, traffic/forensics scripts, `configs/etc/**` | Reproducibility, applied config | I | **High** |
| A-11 | `lab.env` | Root | Single source of truth: subnet, IPs, hostnames, admin user, timezone, IPv6 ULA | Identity/addresses | I | **Critical** — one change reconfigures every stage |
| A-12 | `INTEGRATION.md` | Root | Cross-stage contract v1.0: ports, schema, log transport, changelog | Format/port contracts | I | **Critical** |
| A-13 | Traffic analyzer | Stage 1 | `network_traffic_analyzer.py`, metadata-only, Scapy | Code, capture capability | I/A | **High** |
| A-14 | Detection rules | Stage 1 | `detections.py` — 3 explainable heuristics | Rule logic/thresholds | I | Medium |
| A-15 | Traffic reports | Stage 1 | `traffic_report.json` — schema `traffic-report/1.0`, structured flows, detections | Network metadata (addresses, ports, flows) | C/I | **High** — feeds Stage 7 |
| A-16 | Kali workstation | Stage 1/5/6 | Attacker/client VM, 10.10.10.20; runs traffic generator, later recon/pentest | Tooling, keys | C | **High** — trusted in-lab attacker |
| A-17 | Lab segment | All | Host-only network 10.10.10.0/24; IPv6 ULA fd00:10:10::/48 reserved | Shared transport | A | **High** |
| A-18 | NAT/upstream path | Stage 2 | Outbound-only (package updates, NTP); inbound default-deny | Egress | — | Low |

## B. Placeholder assets (planned stages — reserve IDs now)

| ID | Asset | Owner (planned) | Notes |
|---|---|---|---|
| A-19 | VulnBank web app (80/443) | Stage 4 | Deliberately vulnerable; STRIDE rows arrive with the app |
| A-20 | Database (3306/5432) | Stage 4 | Banking data model |
| A-21 | SIEM collector (514/10514) | Stage 7 | Consumes A-08 (syslog RFC 5424) + A-15 |
| A-22 | Recon tool / reports | Stage 5 | Consumes A-11/A-12 for target scope |
| A-23 | Incident-response artifacts | Stage 8 | Consumes A-08/A-09/A-15 |

## C. Asset inventory rules

1. **Add before you build:** every new module adds its assets here *first*.
2. **One owner:** each asset has exactly one owning stage (the stage that
   creates it); consumers are recorded in the DFD instead.
3. **Sensitivity is stage-relative:** A-16 (Kali) is *trusted in-lab* — its
   confidentiality matters (attacker tools/keys), but it is not a target of
   the model's own attackers.
4. **Never store secrets:** consistent with `lab.env`, no credential value
   ever appears in this inventory.
